<?xml version="1.0" encoding="utf-8"?>
<!--
  One row in the autofill dropdown. Deliberately plain: the dropdown is drawn
  by the target app's process via RemoteViews, so only simple views are safe.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:background="#11131B">

    <ImageView
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_marginEnd="12dp"
        android:src="@drawable/ic_launcher_foreground"
        android:contentDescription="CipherVault" />

    <TextView
        android:id="@+id/autofill_entry_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#F8FAFC"
        android:textSize="15sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="CipherVault" />
</LinearLayout>
